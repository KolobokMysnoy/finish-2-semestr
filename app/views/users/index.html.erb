<p style="color: green"><%= notice %></p>

<h1><%= t(".user")%></h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to (t(".show")), user %>
    </p>
  <% end %>
</div>

<%= link_to t(".reg"), new_user_path %>
